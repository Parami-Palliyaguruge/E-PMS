specVersion: "v1"
serviceId: "pms-app"
location: "asia-east1"
schema:
  source: "./schema"
  datasource:
    postgresql:
      database: "fdcdb"
      cloudSql:
        instanceId: "e-pms-fdc"
      # schemaValidation: "COMPATIBLE"
connectorDirs: ["./connector"]
